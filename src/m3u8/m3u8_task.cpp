#include "m3u8_task.h"

M3u8Task::M3u8Task(QString download_link) {
// file_urls = []
//        for item in playlist.files:
//            if item.find('http://') != 0 and item.find('https://') != 0:
//                file_urls.append(os.path.join(os.path.dirname(download_link), item))
//            else:
//                file_urls.append(item)
}
